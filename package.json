{
  "name": "smillie-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:data": "python3 build_site.py",
    "build:all": "python3 build_site.py && vite build && npx pagefind --site pagefind-source --output-path dist/pagefind",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "vite": "^6.0.0"
  }
}
