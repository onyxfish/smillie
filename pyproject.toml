[project]
name = "smillie"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "anthropic>=0.25",
    "tqdm>=4.0",
]

[project.scripts]
fetch-smillie = "fetch_smillie:main"
transcribe-smillie = "transcribe_smillie:main"
